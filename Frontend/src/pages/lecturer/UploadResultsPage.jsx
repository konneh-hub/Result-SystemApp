import React from 'react';
import { Box, Typography } from '@mui/material';

const UploadResultsPage = () => (
  <Box>
    <Typography variant="h4" sx={{ fontWeight: 700, mb: 3 }}>Upload Results</Typography>
    <Typography>Coming soon - Upload results interface</Typography>
  </Box>
);

export default UploadResultsPage;
