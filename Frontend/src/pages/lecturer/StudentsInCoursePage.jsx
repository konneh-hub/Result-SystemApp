import React from 'react';
import { Box, Typography } from '@mui/material';

const StudentsInCoursePage = () => (
  <Box>
    <Typography variant="h4" sx={{ fontWeight: 700, mb: 3 }}>Students In Course</Typography>
    <Typography>Coming soon - Students in course interface</Typography>
  </Box>
);

export default StudentsInCoursePage;
