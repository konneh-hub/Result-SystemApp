import React from 'react';
import { Box, Typography } from '@mui/material';

const ResultHistoryPage = () => (
  <Box>
    <Typography variant="h4" sx={{ fontWeight: 700, mb: 3 }}>Result History</Typography>
    <Typography>Coming soon - Result history interface</Typography>
  </Box>
);

export default ResultHistoryPage;
