import React from 'react';
import { Box, Typography } from '@mui/material';

const TranscriptRequestsPage = () => (
  <Box>
    <Typography variant="h4" sx={{ fontWeight: 700, mb: 3 }}>Transcript Requests</Typography>
    <Typography>Coming soon - Transcript requests interface</Typography>
  </Box>
);

export default TranscriptRequestsPage;
