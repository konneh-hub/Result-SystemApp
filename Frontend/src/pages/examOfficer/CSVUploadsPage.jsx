import React from 'react';
import { Box, Typography } from '@mui/material';

const CSVUploadsPage = () => (
  <Box>
    <Typography variant="h4" sx={{ fontWeight: 700, mb: 3 }}>CSV Uploads</Typography>
    <Typography>Coming soon - CSV uploads interface</Typography>
  </Box>
);

export default CSVUploadsPage;
