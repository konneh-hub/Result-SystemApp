import React from 'react';
import { Box, Typography } from '@mui/material';

const RejectedResultsPage = () => (
  <Box>
    <Typography variant="h4" sx={{ fontWeight: 700, mb: 3 }}>Rejected Results</Typography>
    <Typography>Coming soon - Rejected results interface</Typography>
  </Box>
);

export default RejectedResultsPage;
