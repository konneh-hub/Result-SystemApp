import React from 'react';
import { Box, Typography } from '@mui/material';

const ApprovedResultsPage = () => (
  <Box>
    <Typography variant="h4" sx={{ fontWeight: 700, mb: 3 }}>Approved Results</Typography>
    <Typography>Coming soon - Approved results interface</Typography>
  </Box>
);

export default ApprovedResultsPage;
