import React from 'react';
import { Box, Typography } from '@mui/material';

const RegisteredCoursesPage = () => (
  <Box>
    <Typography variant="h4" sx={{ fontWeight: 700, mb: 3 }}>Registered Courses</Typography>
    <Typography>Coming soon - Registered courses interface</Typography>
  </Box>
);

export default RegisteredCoursesPage;
