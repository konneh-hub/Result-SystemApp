import React from 'react';
import { Box, Typography } from '@mui/material';

const StudentNotificationsPage = () => (
  <Box>
    <Typography variant="h4" sx={{ fontWeight: 700, mb: 3 }}>Notifications</Typography>
    <Typography>Coming soon - Notifications interface</Typography>
  </Box>
);

export default StudentNotificationsPage;
