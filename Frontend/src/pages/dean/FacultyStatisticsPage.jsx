import React from 'react';
import { Box, Typography } from '@mui/material';

const FacultyStatisticsPage = () => (
  <Box>
    <Typography variant="h4" sx={{ fontWeight: 700, mb: 3 }}>Faculty Statistics</Typography>
    <Typography>Coming soon - Faculty statistics interface</Typography>
  </Box>
);

export default FacultyStatisticsPage;
