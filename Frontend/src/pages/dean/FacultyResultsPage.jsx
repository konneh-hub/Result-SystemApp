import React from 'react';
import { Box, Typography } from '@mui/material';

const FacultyResultsPage = () => (
  <Box>
    <Typography variant="h4" sx={{ fontWeight: 700, mb: 3 }}>Faculty Results</Typography>
    <Typography>Coming soon - Faculty results interface</Typography>
  </Box>
);

export default FacultyResultsPage;
