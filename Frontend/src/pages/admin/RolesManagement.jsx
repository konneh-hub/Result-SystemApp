import React from 'react';
import { Box, Typography } from '@mui/material';

const RolesManagement = () => (
  <Box>
    <Typography variant="h4" sx={{ fontWeight: 700, mb: 3 }}>Roles Management</Typography>
    <Typography>Coming soon - Roles management interface</Typography>
  </Box>
);

export default RolesManagement;
