import React from 'react';
import { Box, Typography } from '@mui/material';

const CoursesManagement = () => (
  <Box>
    <Typography variant="h4" sx={{ fontWeight: 700, mb: 3 }}>Courses Management</Typography>
    <Typography>Coming soon - Courses management interface</Typography>
  </Box>
);

export default CoursesManagement;
